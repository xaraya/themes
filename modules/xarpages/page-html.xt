<xar:comment>    This is a modified version of the original xarpages 'html' page type template. Two things have been added that are important to know:    1. there is an edit link at the top of the page for admin users.    2. a second DD variable has been added called 'pagenote'. In order to take advantage of this you must add a DD field called pagenote in exactly the same manner as you would with the body DD field.    3. The HTML body of the page is placed into a dynamicdata field called    'body'.</xar:comment><xar:comment>    Set the page title to the description of the page.    You could instead create a DD property called 'title' and refererence    it as $current_page.dd.title, reserving the description field for    administration notes.</xar:comment><xar:set name="dummy">#xarTplSetPageTitle($current_page.desc)#</xar:set>		<xar:sec mask="AdminXarpagesPage" catch="false">    <div style="padding: 5px; margin: 10px; background: yellow; border: solid 2px red; color: red; font-weight: bold;">        &raquo;&nbsp;<a href="#xarModURL('xarpages','admin','modifypage',array('pid' => $pid))#">            edit da page        </a>    </div></xar:sec><xar:comment><div>    <xar:mlstring>You are here:</xar:mlstring>    <xar:loop name="$ancestors">        <xar:if condition="$loop:item.pid eq $pid">            &gt;&gt; <span title="#xarVarPrepForDisplay($loop:item.desc)#">#$loop:item.name#</span>        <xar:else />            &gt;&gt; <a href="#xarModURL('xarpages','user','display',array('pid'=&gt;$loop:item.pid))#" title="#xarVarPrepForDisplay($loop:item.desc)#">#$loop:item.name#</a>        </xar:if>    </xar:loop></div></xar:comment><xar:if condition="!empty($siblings)">    <div>        <xar:mlstring>Pages in this section:</xar:mlstring>        <xar:loop name="$siblings">            <xar:if condition="$loop:item.pid eq $pid">                <span title="#xarVarPrepForDisplay($loop:item.desc)#">#$loop:item.name#</span>            <xar:else />                <a href="#xarModURL('xarpages','user','display',array('pid'=&gt;$loop:item.pid))#" title="#xarVarPrepForDisplay($loop:item.desc)#">#$loop:item.name#</a>            </xar:if>        </xar:loop>    </div></xar:if><xar:if condition="!empty($children)">    <div>        <xar:mlstring>Sub-pages:</xar:mlstring>        <xar:loop name="$children">            <a href="#xarModURL('xarpages','user','display',array('pid'=&gt;$loop:item.pid))#" title="#xarVarPrepForDisplay($loop:item.desc)#">#$loop:item.name#</a>        </xar:loop>    </div></xar:if><xar:if condition="isset($current_page.dd.body)">    <div style="margin: 1em; padding: 1em; color: #000; font-size: 12px !important; ">#$current_page.dd.body#</div><xar:else />    <div class="xar-error"><xar:mlstring>Please create a Dynamic Data item 'body' to hold your HTML</xar:mlstring></div></xar:if><xar:if condition="!empty($current_page.dd.pagenote)">    <div style="margin: 50px;  " >        <fieldset style="font-size: 10px; color: #666; border: solid 1px #e0e0e0; background: #fff; width: 40%;">            <legend style="color: #666; font-weight: bold; font-size: 12px; border: solid 1px #e0e0e0; padding: 3px 6px; background: #f0f0f0;">                NOTES            </legend>            <em>#$current_page.dd.pagenote#</em>        </fieldset>       </div></xar:if>